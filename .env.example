# Connection string de Neon (o Supabase) — Postgres.
# Se configura como variable de entorno en Vercel, nunca se commitea.
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"
